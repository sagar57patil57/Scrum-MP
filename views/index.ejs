<% include ./header %>

<% if(session.userId){ %>

<div class="container">
	<br><br><br><br>
    <div class="row">

    	<div class="col-lg-10 col-md-10 mx-auto">
    		<% if( session.userId.toString() === '5cbb61dcd02c321d1d03cf1e' ) {  %>
            <label class="d-flex justify-content-center"><img src="https://img.icons8.com/color/96/000000/admin-settings-male.png"></label><br/>
			<a href="/dashboard/man" class="d-flex justify-content-center">GO TO ADMIN DASHBOARD</a>
			<% } else { %>
            <label class="d-flex justify-content-center">Nice to see you again,</label><br/>
			<a href="/dashboard/emp" class="d-flex justify-content-center">GO TO DASHBOARD</a>
    		<% } %>
    	</div>
    </div>
</div>

<% } else { %>

    <br/><br/>
	<label class="d-flex justify-content-center"><img src="https://img.icons8.com/ios/100/000000/enter-2-filled.png"></label><br/>
    <a href="/user/login" class="d-flex justify-content-center">YOU NEED TO LOGIN</a>
        <!-- Button trigger modal -->
        <div class="d-flex justify-content-center">
    <button type="button" class="btn" data-toggle="modal" data-target="#basicExampleModal">
      About
    </button>

    <!-- Modal -->
    <div class="modal fade" id="basicExampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
      aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            The Features are : 
            <p>2 logins</p>
            <p>Add projects and issues</p>
            <p>View projects and issues</p>
            <p>Chat</p>
            <p>Search</p>
            <p>Assign issues</p>
            <p>Change issue status</p>
            <p>comments</p>
            <p>Users list and profile</p>
            <p>edit profile</p>
            <p>...much more</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn" data-dismiss="modal">Close</button>
          </div>
        </div>
        </div>
      </div>
    </div>

<% } %>

<% include ./footer %>